<%= render partial: "link", locals: { links: ["ermahgerd", "chef", "pirate", "fudd"], pokemon: params[:id] } %>

<h1><%= @pokemon.select_entry(@language).name %></h1>
<%= image_tag(@pokemon.sprite) %>
<h2><%= @pokemon.number %></h2>
<p><%= @pokemon.select_entry(@language).description %></p>
</ul>

<%= link_to "Back to Pokedex", pokemons_path %>